{/* {this.state.places.map( place =>{
                   return(
                       <View key={place.id}>
                            <Text style={styles.placename}>{place.name}</Text>
                       </View>  
                   )

               })} */}